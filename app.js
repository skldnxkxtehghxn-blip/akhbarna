let all=[],cat="الكل";async function load(){try{const r=await fetch("news.json?"+Date.now());all=await r.json();render()}catch(e){document.getElementById("status").textContent="تعذر تحميل الأخبار حالياً."}}function render(){const x=all.filter(n=>cat==="الكل"||n.category===cat);document.getElementById("news").innerHTML=x.map(n=>`<article class="card"><div class="pic">${n.image?`<img src="${n.image}" style="width:100%;height:100%;object-fit:cover">`:"📰"}</div><div class="body"><div class="tag">${n.category}</div><h2>${n.title}</h2><p>${n.summary}</p><div class="date">${n.source} — ${n.date}</div><a href="${n.url}" target="_blank" rel="noopener">قراءة المصدر ↗</a></div></article>`).join("");document.getElementById("status").textContent=`تم التحديث: ${all[0]?.updated||"غير معروف"}`}document.querySelectorAll("nav a").forEach(a=>a.onclick=e=>{e.preventDefault();cat=a.dataset.cat;render()});load();